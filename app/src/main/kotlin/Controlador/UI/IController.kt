package Controlador.UI

interface IController {

    /**
     * Esta función se encargará de realizar todas
     * las operaciones que permitan al controlador
     * prepararse para cuando le llamen a iniciarse
     */
    fun preCargar()

}